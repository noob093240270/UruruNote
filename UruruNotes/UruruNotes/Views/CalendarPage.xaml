<Window x:Class="UruruNotes.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UruruNotes" 
      xmlns:converters="clr-namespace:UruruNotes.Models" xmlns:viewsmodels="clr-namespace:UruruNotes.ViewsModels" d:DataContext="{d:DesignInstance Type=viewsmodels:CalendarViewModel}"
        mc:Ignorable="d" 
      MinHeight="450" MaxHeight="650" MinWidth="450" MaxWidth="850"
      Title="CalendarPage" Icon="/Views/calendar.png">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToBrushConverter x:Key="BoolToBrushConverter" />
    </Window.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="400px"/>
            <!-- Календарь -->
            <ColumnDefinition Width="auto" x:Name="NotesColumn"/>
            <!-- Панель задач -->
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- Для заголовка и кнопок -->
                <RowDefinition Height="Auto" />
                <!-- Для дней недели -->
                <RowDefinition Height="*" />
                <!-- Для календаря -->
            </Grid.RowDefinitions>

            <!-- Заголовок и кнопки -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" Grid.Row="0">
                <Button Content="⟨" Width="50" Height="30" Command="{Binding PreviousMonthCommand}" Background="White" BorderBrush="#FF235C9A" />
                <TextBlock Text="{Binding CurrentMonthYear}" FontSize="20" FontWeight="Bold" Margin="20,0" />
                <Button Content="⟩" Width="50" Height="30" Command="{Binding NextMonthCommand}" Background="White" BorderBrush="#FF235C9A" />
            </StackPanel>

            <!-- Сетка для дней недели -->
            <UniformGrid Rows="1" Columns="7" Margin="10" Grid.Row="1">
                <TextBlock Text="Пн" TextAlignment="Center" FontWeight="Bold" />
                <TextBlock Text="Вт" TextAlignment="Center" FontWeight="Bold" />
                <TextBlock Text="Ср" TextAlignment="Center" FontWeight="Bold" />
                <TextBlock Text="Чт" TextAlignment="Center" FontWeight="Bold" />
                <TextBlock Text="Пт" TextAlignment="Center" FontWeight="Bold" />
                <TextBlock Text="Сб" TextAlignment="Center" FontWeight="Bold" Foreground="Red" />
                <TextBlock Text="Вс" TextAlignment="Center" FontWeight="Bold" Foreground="Red" />
            </UniformGrid>

            <!-- Календарь -->
            <ItemsControl ItemsSource="{Binding Days}" Grid.Row="2" Margin="0,0,10,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="6" Columns="7" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.OpenTaskAreaCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                CommandParameter="{Binding}" 
                Padding="5" Margin="2"
                HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                Background="{Binding IsCurrentMonth, Converter={StaticResource BoolToBrushConverter}}"
                BorderBrush="Transparent" BorderThickness="0"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Grid>
                                <!-- Определяем строки для разделения элементов -->
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <!-- Для числа дня -->
                                    <RowDefinition Height="Auto" />
                                    <!-- Для красной точки и времени -->
                                </Grid.RowDefinitions>

                                <!-- Число дня (в первой строке) -->
                                <TextBlock Text="{Binding DisplayText}" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                           FontSize="14" Grid.Row="0" />

                                <!-- Контейнер для красной точки и времени напоминания (во второй строке) -->
                                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center" VerticalAlignment="Center" 
                            Grid.Row="1" Margin="0,5,0,0">
                                    <!-- Красная точка, если есть заметка -->
                                    <Ellipse Width="6" Height="6" Fill="Red" VerticalAlignment="Center"
                             Visibility="{Binding HasNote, Converter={StaticResource BoolToVisibilityConverter}}" />

                                    <!-- Время напоминания, если оно есть -->
                                    <TextBlock Text="{Binding ReminderTime}" VerticalAlignment="Center" 
                               Foreground="Red" FontSize="10" Margin="5,0,0,0" />
                                </StackPanel>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>



        <Border Grid.Column="1"  Width="Auto" 
            Visibility="{Binding IsTaskPanelVisible, Converter={StaticResource BoolToVisibilityConverter}}" 
            Margin="10">
            <Grid Margin="-39,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Для кнопки -->
                    <ColumnDefinition x:Name="TaskPanelColumn" Width="Auto"/>
                    <!-- Для панели -->
                </Grid.ColumnDefinitions>
                <Button x:Name="ToggleButton" Grid.Column="0"
        Width="20" Height="50"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Background="Transparent"
        Click="TogglePanel_Click"
        BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <!-- Прямоугольник-закладка -->
                                <Rectangle x:Name="BookmarkRectangle"
                           Width="20" Height="50"
                           Fill="LightGray" Stroke="Gray"
                           StrokeThickness="1"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center" />
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <StackPanel x:Name="TaskPanel" Grid.Column="1" Background="LightGray" Width="285" HorizontalAlignment="Center">
                    <TextBlock Text="Создать заметку" FontSize="20" FontWeight="Bold" Margin="10"/>
                    <TextBox TextWrapping="Wrap" Text="{Binding NewTaskContent}" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto" MaxWidth="278" Height="100" Margin="10" FontSize="14" CaretBrush="#FF235C9A" SelectionBrush="#FF235C9A" PreviewKeyDown="TextBox_PreviewKeyDown" Loaded="TextBox_Loaded" />
                    <Button Content="Сохранить"  Command="{Binding SaveNoteCommand}" Width="110" Margin="10" HorizontalAlignment="Left" Background="White" BorderBrush="#FF235C9A" />

                    <TextBlock Text="Создать напоминание" FontSize="20" FontWeight="Bold" Margin="10"/>
                    <TextBox TextWrapping="Wrap" Text="{Binding NewTaskContentRemind}"  AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto" MaxWidth="278" Height="100" Margin="10" FontSize="14" CaretBrush="#FF235C9A" SelectionBrush="#FF235C9A" PreviewKeyDown="TextBox_PreviewKeyDown" Loaded="TextBox_Loaded" />
                    <TextBlock Text="Время напоминания:" FontSize="14" Height="20" Width="133" Margin="10,5,10,0" HorizontalAlignment="Right"/>
                    <StackPanel Orientation="Horizontal" Margin="10,5,10,10">
                        <Button Content="Сохранить"  Command="{Binding SaveReminderCommand}" Width="110" Margin="10" Background="White" BorderBrush="#FF235C9A" HorizontalAlignment="Left" />
                        <!-- Выбор времени для напоминания -->
                        <!-- Выбор часов -->
                        <ComboBox x:Name="HourPicker" SelectedItem="{Binding SelectedHour}" Width="60" VerticalAlignment="Center" Margin="10,0,5,0">
                            <ComboBox.ItemsSource>
                                <x:Array Type="sys:Int32" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                    <sys:Int32>0</sys:Int32>
                                    <sys:Int32>1</sys:Int32>
                                    <sys:Int32>2</sys:Int32>
                                    <sys:Int32>3</sys:Int32>
                                    <sys:Int32>4</sys:Int32>
                                    <sys:Int32>5</sys:Int32>
                                    <sys:Int32>6</sys:Int32>
                                    <sys:Int32>7</sys:Int32>
                                    <sys:Int32>8</sys:Int32>
                                    <sys:Int32>9</sys:Int32>
                                    <sys:Int32>10</sys:Int32>
                                    <sys:Int32>11</sys:Int32>
                                    <sys:Int32>12</sys:Int32>
                                    <sys:Int32>13</sys:Int32>
                                    <sys:Int32>14</sys:Int32>
                                    <sys:Int32>15</sys:Int32>
                                    <sys:Int32>16</sys:Int32>
                                    <sys:Int32>17</sys:Int32>
                                    <sys:Int32>18</sys:Int32>
                                    <sys:Int32>19</sys:Int32>
                                    <sys:Int32>20</sys:Int32>
                                    <sys:Int32>21</sys:Int32>
                                    <sys:Int32>22</sys:Int32>
                                    <sys:Int32>23</sys:Int32>
                                </x:Array>
                            </ComboBox.ItemsSource>
                        </ComboBox>

                        <!-- Выбор минут -->
                        <ComboBox x:Name="MinutePicker" SelectedItem="{Binding SelectedMinute}" Width="60" VerticalAlignment="Center" Margin="0,0,10,0">
                            <ComboBox.ItemsSource>
                                <x:Array Type="sys:Int32" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                    <sys:Int32>0</sys:Int32>
                                    <sys:Int32>5</sys:Int32>
                                    <sys:Int32>10</sys:Int32>
                                    <sys:Int32>15</sys:Int32>
                                    <sys:Int32>20</sys:Int32>
                                    <sys:Int32>25</sys:Int32>
                                    <sys:Int32>30</sys:Int32>
                                    <sys:Int32>35</sys:Int32>
                                    <sys:Int32>40</sys:Int32>
                                    <sys:Int32>45</sys:Int32>
                                    <sys:Int32>50</sys:Int32>
                                    <sys:Int32>55</sys:Int32>
                                </x:Array>
                            </ComboBox.ItemsSource>
                        </ComboBox>

                    </StackPanel>


                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
